name: SimpleAlarm
version: 1.0.0

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_local_notifications: ^9.0.0
  audio_players: ^0.20.1
  sqflite: ^2.0.0+4
  flutter_localizations:
    sdk: flutter
  intl: ^0.17.0

flutter:
  uses-material-design: true
  generate: 
    - lib/l10n/*.arb
